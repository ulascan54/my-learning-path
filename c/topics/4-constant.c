#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main()
{
    // constant = fixed value that cannot be altered by the program during its execution
    const float PI;
    // PI = 420.60-> We cannot do that

    printf("%f", PI);
    return 0;
}