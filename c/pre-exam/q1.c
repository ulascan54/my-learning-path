#include <stdio.h>
#include <stdlib.h>

int foo(const int arr[], int size);

int main()
{
    /*
    int my_function( int ) finds whether an integer is a magic number or not (returns 1 if so, O if not).
    Using the above function, write a function foo, which finds the # of magic numbers in an integer array.
    Just write the prototype of the function foo.
    Warning: When I say just write this part, you should only write that part. Providing unnecessary statements results in ZERO points (like providing the whole definition of a function)!!!
        */

        return 0;
}
